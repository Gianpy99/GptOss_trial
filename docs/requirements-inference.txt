# Requirements per ambiente inference (GPU)
# Versione leggera solo per inferenza con UI

# Core ML
torch>=2.0.0
transformers>=4.40.0
accelerate>=0.20.0
bitsandbytes>=0.41.0
peft>=0.10.0

# UI Web
gradio>=4.0.0

# Utilities
python-dotenv
